# cmpatino.github.io

Personal website for [cmpatino.github.io](https://cmpatino.github.io/).

The site is intentionally small:

- `/` for the front page
- `/blog/` and dated blog posts
- `/publications/` kept available but hidden from the navbar

Build locally with:

```sh
bundle install
bundle exec jekyll build
```
